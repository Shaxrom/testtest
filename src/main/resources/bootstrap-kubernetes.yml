spring:
  cloud:
    kubernetes:
      config:
        name: merchants-dev-config
        namespace: default
      client:
        namespace: default
      enabled: true
      secrets:
        namespace: default
      discovery:
        all-namespaces: true
      reload:
        enabled: true